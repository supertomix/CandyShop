@model HomeViewModel

<h1>Популярное</h1>
<div class="row mt-5 mb-3">
    @{
        foreach (Product product in Model.FavProduct)
        {
            @Html.Partial("AllProducts", product)
        }
    }
</div>
